public class HTMLParser {

    /**
     * Extracts a recipe in String format from a valid url.
     * @param url - represents the link to the cooking recipe HTML site.
     * @return A String array with String[0] being the ingredients and String[1] the recipe instructions.
     */
    public static String[] extractRecipe(String url) {
        return new String[] {""};
    }
}
